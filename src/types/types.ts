export type SkillCardType = {
  image: string;
  name: string;
};

export type ProjectCardType = {
  image: string;
  name: string;
  tech: string;
  description: string;
  deploy: string;
  gh: string;
};
